{% extends 'base.html.twig' %}

{% block title %}Moyen{% endblock %}

{% block body %}
    <h1>Moyen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moyen.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ moyen.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moyen_index') }}">back to list</a>

    <a href="{{ path('moyen_edit', {'id': moyen.id}) }}">edit</a>

    {{ include('moyen/_delete_form.html.twig') }}
{% endblock %}
