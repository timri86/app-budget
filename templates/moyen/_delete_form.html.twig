<form method="post" action="{{ path('moyen_delete', {'id': moyen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moyen.id) }}">
    <button class="btn">Delete</button>
</form>
